# D1.2 Tempo Deployment - Verification Instructions

## Status: ✅ DEPLOYED & OPERATIONAL

### What Was Deployed:
1. **Tempo** service (Grafana Tempo latest) on port 3200
2. **OTel Collector** configured to export traces to Tempo via gRPC (port 4317)
3. **Prometheus** configured with exemplar storage (100,000 exemplars)
4. **Grafana** pre-configured with Tempo datasource

### Configuration Changes:
- Created `monitoring/tempo/tempo.yaml` with OTLP receivers and local storage
- Updated `docker-compose.yml`: Fixed RAG API OTel endpoint to use gRPC port 4317
- Updated `monitoring/prometheus/prometheus.yml`: Added exemplar_storage config
- OTel Collector already configured to export to `tempo:4317` via `otlp/tempo` exporter

### Verification Completed:

#### ✅ 1. OTel Collector Receiving & Exporting Traces
```
docker logs rag-otel-collector 2>&1 | grep -E "Span|Trace" | tail -10
```
**Result:**
```
Span #0
    Trace ID       : 1ec54f3923f375905acfcf0b64949bad
Span #1
    Trace ID       : 1ec54f3923f375905acfcf0b64949bad
Span #2
    Trace ID       : 1ec54f3923f375905acfcf0b64949bad
2025-11-10T23:53:39.122Z	info	Traces	{"otelcol.signal": "traces", "resource spans": 1, "spans": 3}
```

#### ✅ 2. Tempo Ingesting & Storing Traces
```
docker logs rag-tempo 2>&1 | tail -10
```
**Result:**
```
level=info ts=2025-11-10T23:52:19.100754078Z caller=instance.go:293 tenant=single-tenant msg="head block cut" block=59dd522d-41b7-457c-86d0-a74b7a0b4e00 size=803157
level=info ts=2025-11-10T23:52:25.839570897Z caller=flush.go:283 msg="block completed" tenant=single-tenant blockID=59dd522d-41b7-457c-86d0-a74b7a0b4e00 duration=93.842637ms
```

#### ✅ 3. Grafana Tempo Datasource Configured
```
curl -s 'http://16.146.148.184:3001/api/datasources/uid/tempo' | jq '{name, type, url}'
```
**Result:**
```json
{
  "name": "Tempo",
  "type": "tempo",
  "url": "http://tempo:3200"
}
```

#### ✅ 4. RAG API Generating Traces
```
curl -s -X POST http://16.146.148.184:3000/api/v1/rag/query \
  -H 'Content-Type: application/json' \
  -d '{"query":"What is agentic chunking?","user_id":"demo","groups":[]}' | jq '.trace_id'
```
**Result:**
```
"262707816422736215842796968171100684872"
```

### Manual Verification Steps (for screenshot):

1. **Open Grafana**: http://16.146.148.184:3001
   - Username: `admin`
   - Password: `admin`

2. **Navigate to Explore**:
   - Click "Explore" in left sidebar
   - Select "Tempo" datasource

3. **Search for Recent Traces**:
   - Query type: "Search"
   - Service Name: "rag-api"
   - Click "Run Query"

4. **View a Trace**:
   - Click on any trace in the results
   - Verify you see spans like:
     - `rag.query` (root span)
     - `retrieve_internal.vector`
     - `retrieve_web.searxng`
     - `synthesis_v1`

5. **Screenshot Required**:
   - Take screenshot showing the full trace tree with all spans visible
   - Save as `D1.2_tempo_trace_screenshot.png`

### Expected Span Structure:
```
rag.query (root)
├── retrieve_internal.vector
├── retrieve_web.searxng
└── synthesis_v1
```

### Acceptance Criteria: ✅ MET
- [x] Tempo service running and healthy
- [x] OTel Collector exporting traces to Tempo successfully
- [x] Tempo ingesting and storing traces (803KB block created)
- [x] Grafana configured with Tempo datasource
- [x] RAG API generating traces with trace_id in response
- [ ] **PENDING**: Screenshot of full trace in Grafana (manual step)

### Next Steps:
- User to access Grafana and capture screenshot of trace
- Then proceed to D1.3 (Frontend OpenTelemetry)

